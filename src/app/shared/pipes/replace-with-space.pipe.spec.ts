import { ReplaceWithSpacePipe } from './replace-with-space.pipe';

describe('ReplaceWithSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWithSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
